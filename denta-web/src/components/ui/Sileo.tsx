import { Toaster } from "sileo";

export default function SileoToaster() {
    return (
        <Toaster
            position="top-right"
            theme="light"
        // You can add more props here if needed based on the library docs
        />
    );
}
